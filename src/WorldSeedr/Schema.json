Possibility = {
    "width": Number,
    "height": Number,
    "children": {
        // "orientation": ["vertical", "horizontal"],
        // "snap": ["top", "right", "bottom", "left"], // optional
        "choices": [Choice, ...]
    }
};

Choice = {
    "percent": Number,
    "type": ["Known", "Random"],
    "title": String,
    "arguments": Object // optional
};



// Should there be a start/end? like in multiple sprites

// problem - things should ALWAYS declare width & height ahead of time