Possibility = {
    "width": Number,
    "height": Number,
    // "widthConstant": Boolean,
    // "heightConstant": Boolean,
    "contents": {
        "direction": ["top", "right", "bottom", "left"],
        "mode": ["Random", "Certain"],
        "spacing": Spacing, // optional
        "children": [Child, ...]
    }
};

Child = {
    "percent": Number, // Required if Possibility.children.mode = "Random"
    "type": ["Known", "Random"],
    "title": String,
    "stretch": { // optional
        "width": Boolean, // optional
        "height": Boolean // optional
    },
    "arguments": Object // optional
};

Spacing = one of...
    Number
    [Number, Number]
    [Choice, ...]
    {
        "min": Number,
        "max": Number,
        "units": Number // optional
    }



// Should there be a start/end? like in multiple sprites

// Should add possibility for overlap (think scenery)
// Should add possibility for varying width/height (think random area gaps)